algoritmo "contador"
var
    inicial: inteiro
    fim: inteiro
inicio
    escreva("Numero inicial: ")
    leia(inicial)
    escreva("Numero Final: ")
    leia(fim)
    enquanto inicial <= fim faca
        escreva(inicial)
        inicial <- inicial + 1
    fimenquanto

fimalgoritmo